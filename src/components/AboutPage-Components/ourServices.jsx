import { SliderAPI, SlidingCard } from "../homePage-Components/OurProducts";

const OurServices = function () {
  return (
    <section className="py-16 bg-gradient-to-b from-gray-800 to-gray-900 relative">
    </section>
  );
};

export default OurServices;
